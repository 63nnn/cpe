#include <iostream>
using namespace std;

int fib_bits = 0;

int make_Fib(int x)
{
    char bits[100];

    return;
}

int main()
{
    int n;
    cin >> n;
    int nums[n], biggest = 0;
    for (int i = 0; i < n; i++)
    {
        int tmp;
        cin >> tmp;
        nums[i] = tmp;
        if (nums[i] > biggest)
        {
            biggest = nums[i];
        }
    }

    int fib[10000];

    return 0;
}